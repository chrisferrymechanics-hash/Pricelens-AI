// ProductID.swift
extension String {
    // Subscriptions
    static let proMonthly   = "PcLnsPremSub"   // ← replace with your Product ID
   

    // Consumables (e.g. coin packs, credit bundles — can be purchased multiple times)
    static let coins100     = "PrLnsCrPck"     // ← replace or remove
    

    // Non-consumables (one-time purchases that unlock a feature permanently)
    
}
